# Sisko

A desktop Nushell workbench: a full Nushell session with a professional table UI.

See [CONTEXT.md](CONTEXT.md) for the domain language and `docs/adr/` for the architectural decisions.

```sh
cargo run
```

Type a pipeline in the REPL bar and press Enter. Each Invocation creates a new Result above the bar.
